defmodule DtWeb.PageControllerTest do
  use DtWeb.ConnCase

  test "GET /" do
    assert true
    # TODO: fix webapp deploy path
    # conn = get build_conn, "/"
    # assert html_response(conn, 200)
  end
end
